# ── Dependencies ──
node_modules/

# ── Environment variables ──
.env
.env.local
.env.*.local

# ── Build outputs ──
dist/
build/

# ── Prisma ──
# Keep migrations in git — do not ignore them
# Only ignore generated client if you regenerate on deploy
# backend/src/generated/

# ── Logs ──
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo

# ── TypeScript ──
*.tsbuildinfo

# ── Vite ──
.vite/

# ── Test coverage ──
coverage/

# ── Misc ──
*.local